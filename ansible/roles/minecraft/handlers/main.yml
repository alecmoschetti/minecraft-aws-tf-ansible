# ***********************************************
# Handler tasks triggered by systemd notify tasks
# These are necessary to run after systemd service
# files are changed.
# ***********************************************
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart minecraft
  ansible.builtin.systemd:
    name: minecraft
    state: restarted
